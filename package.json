{
  "name": "follow-immo",
  "version": "1.0.0",
  "description": "Follow Immo - Real estate tracking platform with Chrome extension and back office",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "npm run build:extension && npm run build:back-office",
    "build:extension": "cd src/extension && npm install && npm run build",
    "build:back-office": "echo 'Back office build not implemented yet'",
    "dev": "npm run dev:extension",
    "dev:extension": "cd src/extension && npm install && npm run dev",
    "install:all": "npm install && cd src/extension && npm install"
  },
  "workspaces": [
    "src/extension"
  ]
}
